# Untitled notebook

```elixir
Mix.install([
  {:nx, "~> 0.5"},
  {:kino, "~> 0.9.0"},
  {:fleet_monitor, path: __DIR__}
])
```

## Section

```elixir
FleetMonitor.get_image()
|> Base.decode64!()
|> Kino.Image.new(:jpg)
```

```elixir
:observer.start()
```

```elixir
ImageLoop.loop(10)
```

```elixir
File.read!("data.txt")
|> String.split(" ")
|> Enum.at(7)
|> Base.decode64!()
|> Kino.Image.new(:jpg)
```
