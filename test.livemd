# Untitled notebook

```elixir
Mix.install([
  {:nx, "~> 0.5"},
  {:kino, "~> 0.9.0"}
])
```

## Section

```elixir
File.read!("data.txt")
|> String.split(" ")
|> Enum.at(7)
|> Base.decode64!()
|> Kino.Image.new(:jpg)
```
